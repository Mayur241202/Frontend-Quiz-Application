import React from 'react';
import { motion } from 'framer-motion';

const OptionButton = ({ option, isSelected, onClick, delay }) => {
  return (
    
      {option}
    
  );
};

export default OptionButton;